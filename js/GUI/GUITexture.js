class GUITexture {
    constructor(texture, position, scale) {
        this.texture = texture;
        this.position = position;
        this.scale = scale;
    }
}

module.exports = {
    GUITexture,
};
